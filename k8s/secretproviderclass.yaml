# Azure Key Vault SecretProviderClass
# Uses Azure Key Vault Provider for Secrets Store CSI Driver
# Placeholders will be replaced by CI/CD pipeline:
# - ${KEYVAULT_NAME} - Azure Key Vault name
# - ${TENANT_ID} - Azure tenant ID
# - ${MANAGED_IDENTITY_CLIENT_ID} - Managed identity client ID (REQUIRED when multiple identities exist)
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: authmanager-azure-keyvault
  namespace: muzika
  labels:
    app: authmanager
    app.kubernetes.io/name: authmanager
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "true"
    userAssignedIdentityID: "${MANAGED_IDENTITY_CLIENT_ID}"
    keyvaultName: "${KEYVAULT_NAME}"
    tenantId: "${TENANT_ID}"
    objects: |
      array:
        - |
          objectName: mysql-connection-string
          objectType: secret
          objectAlias: MYSQL_URL
        - |
          objectName: mysql-username
          objectType: secret
          objectAlias: MYSQL_USERNAME
        - |
          objectName: mysql-password
          objectType: secret
          objectAlias: MYSQL_PASSWORD
        - |
          objectName: jwt-secret
          objectType: secret
          objectAlias: JWT_SECRET
  secretObjects:
    - secretName: authmanager-secrets
      type: Opaque
      data:
        - objectName: MYSQL_URL
          key: MYSQL_URL
        - objectName: MYSQL_USERNAME
          key: MYSQL_USERNAME
        - objectName: MYSQL_PASSWORD
          key: MYSQL_PASSWORD
        - objectName: JWT_SECRET
          key: JWT_SECRET
